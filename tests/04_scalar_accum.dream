// Scalar accumulators are native doubles (no tensor boxing, no graph).
// EXPECT: 1000000
fn app() {
    let t = 0.0;
    let i = 0;
    while (i < 1000000) { t = t + 1.0; i = i + 1; }
    println(t);
    return 0.0;
}
